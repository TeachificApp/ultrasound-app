{
  "type": "object",
  "properties": {
    "questions": {
      "type": "array",
      "minItems": 7,
      "maxItems": 7,
      "items": {
        "type": "object",
        "properties": {
          "question": { "type": "string", "minLength": 12 },
          "options": { "type": "array", "minItems": 4, "maxItems": 4, "items": { "type": "string", "minLength": 1 } },
          "correctAnswer": { "type": "string", "minLength": 1 },
          "explanation": { "type": "string", "minLength": 12 },
          "correctFeedback": { "type": "string", "minLength": 12 },
          "incorrectFeedback": { "type": "string", "minLength": 12 }
        },
        "required": ["question", "options", "correctAnswer", "explanation", "correctFeedback", "incorrectFeedback"],
        "additionalProperties": false
      }
    }
  },
  "required": ["questions"],
  "additionalProperties": false
}
