{
  "type": "object",
  "properties": {
    "reviews": {
      "type": "array",
      "minItems": 50,
      "maxItems": 50,
      "items": {
        "type": "object",
        "properties": {
          "index": { "type": "integer", "minimum": 0, "maximum": 349 },
          "verdict": { "type": "string", "enum": ["approve", "reject"] },
          "reason": { "type": "string", "minLength": 4 }
        },
        "required": ["index", "verdict", "reason"],
        "additionalProperties": false
      }
    }
  },
  "required": ["reviews"],
  "additionalProperties": false
}
