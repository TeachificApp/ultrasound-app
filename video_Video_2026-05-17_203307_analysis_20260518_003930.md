Based on the video, here is a description of what happens during those two specific flows:

**1. Adding Blocks in the Course Overview Editor (Crash)**
*   The user navigates to the "Course Overview" tab and clicks the "+ Add Block" button within the editable content area.
*   A modal appears with layout options. The user clicks on the "Text / Image" block option.
*   **Crash:** Immediately upon clicking the option, the application crashes. The screen turns white and displays a long list of raw code/error logs (stack trace) along with a green "Reload Page" button.
*   When the user clicks "Reload Page," the application reloads and drops them back out to the main "Education Library" course list, rather than returning them to the editor.

**2. Students / Enroll Student Flow (Unexpected Behavior)**
*   The user navigates to the "Students" tab and clicks the "+ Enroll Student" button.
*   An "Enroll a Student" modal opens, prompting the user to "Search by name or email."
*   The user types a full email address (`tarabryson@gmail.com`) into the search field.
*   **Unexpected Behavior:** The system displays a message saying "No users found matching 'tarabryson@gmail.com'". Crucially, the "Enroll Student" button remains disabled (greyed out). The user clicks the disabled button multiple times, but nothing happens.
*   It appears the system only allows enrolling *existing* users who are already registered on the platform. There is no option provided in this modal to invite or create an account for a new user via email, forcing the user to close the modal without completing the task.