Based on the video provided, here is a description of the issues you mentioned:

**1) Rich Text Editor Image Resize/Adjustment Problem:**
This issue is **not shown** in the video. The user's interactions are entirely focused on clicking the navigation and settings buttons in the top right header. At no point does the user interact with the "Lesson Description" rich text editor or attempt to insert, resize, or adjust an image within it.

**2) Lesson Editor Header Button Layout Issue:**
The video clearly demonstrates a layout instability in the top right header area. As the user navigates through different lessons using the "Previous" (`<`) and "Next" (`>`) buttons, the layout of the buttons changes dynamically. 

Specifically, when the user navigates to a quiz lesson (around the 00:05 mark), a new **"Quiz Builder"** tab appears on the far right. Because this new button is added to the row, it forces the existing buttons—"Settings," "Lesson Editor," and the Previous/Next navigation buttons—to shift to the left to make room. 

When the user continues navigating and leaves the quiz lesson (around 00:11), the "Quiz Builder" tab disappears, and all the remaining buttons shift back to the right. This constant shifting of the button positions depending on the lesson type creates an inconsistent layout where the prev/next and settings tabs are constantly moving around.